name: Build Push to Quay
on:
  push:
    paths:
    - azlinux-jupyter-nextflow/Dockerfile
    - .github/workflows/build_jupyter_nextflow_image.yml

jobs:
  jupyter-nextflow:
    name: Build and Push AmazonLinux jupyter-nextflow
    uses: uc-cdis/.github/.github/workflows/image_build_push.yaml@master
    with:
      DOCKERFILE_LOCATION: "./azlinux-jupyter-nextflow/Dockerfile"
      DOCKERFILE_BUILD_CONTEXT: "./azlinux-jupyter-nextflow"
      OVERRIDE_REPO_NAME: "azlinux-jupyter-nextflow"
      OVERRIDE_TAG_NAME: "nextflow-$(echo ${GITHUB_REF#refs/*/} | tr / _)"
      USE_QUAY_ONLY: true
    secrets:
      ECR_AWS_ACCESS_KEY_ID: ${{ secrets.ECR_AWS_ACCESS_KEY_ID }}
      ECR_AWS_SECRET_ACCESS_KEY: ${{ secrets.ECR_AWS_SECRET_ACCESS_KEY }}
      QUAY_USERNAME: ${{ secrets.QUAY_USERNAME }}
      QUAY_ROBOT_TOKEN: ${{ secrets.QUAY_ROBOT_TOKEN }}
